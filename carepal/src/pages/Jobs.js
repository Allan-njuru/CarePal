import Typography from '@mui/material/Typography';

function Jobs() {
  return (
    <div>
      <Typography variant="h4" gutterBottom>Jobs</Typography>
      <Typography>Find caregiver and nursing jobs here.</Typography>
    </div>
  );
}

export default Jobs;